# 2026-08-04 工作日志

## 再塑法典 - 聚焦视图日期导航 + 空心椭圆条件修正

### 需求1：聚焦视图加日期导航（参考日历软件）
**第一版**（已废弃）：按天翻，布局内联
**修正版**（最终）：
- 往前/往后改为**按周翻**（delta * 7 天）
- 标题区改为 flex 三段布局：`[← 返回]  [‹] 日期 [›]  [今天]`，日期居中
- `workTitle` 在 focus 时去掉 `page-title-text` 类，设置 `display:flex;justify-content:space-between`
- 返回 overview 时恢复 `page-title-text` 类和默认样式
- 「今天」按钮在当前日期就是今天时隐藏

### 需求2：空心椭圆条件修正
**第一版**（已废弃）：无标签时显示空心椭圆
**修正版**（最终）：只有标题、无任何详情（工作内容/问题/成长/下一步都没有填）时显示空心椭圆，标签写没写不影响
- 判断条件从 `hasTags` 改为 `hasDetail`（即 `!!detailHtml`）
- 提取 `hasEventDetail(evt)` 辅助函数供其他模块复用

### 需求3：日历网格圆点提示 + 筛选结果列表日期跳转
**实现**（app.js + CSS）：
- 新增 `hasEventDetail(evt)` 辅助函数：判定工作事件是否有详情（工作内容/问题/成长/下一步 任一不为空）
- `renderWorkCalendar` 预计算 `emptyTitleDates` Set，当天有"只有标题"事件则记录
- 日历单元格左下方增加 `.cell-empty-dot`：5×5 空心圆点（绿色边框 + 白色背景）
- 重构左下布局：用 `.cell-bottom-left` flex 容器包裹 pending 数字和 empty-dot，两者并排显示
- `renderWorkEventCard` 增加 `showDate` 可选参数
- `renderWorkTagFilter` 传入 `showDate=true`，在筛选结果卡片标题旁加 `📅 日期` 链接
- 点击日期链接调用 `switchWorkView('focus', dateStr)` 跳转到该日期的聚焦视图

### 待办
- ⚠️ 待V酱确认后手动同步到 ability-lines 并 git 部署（V酱要求不自动上传）