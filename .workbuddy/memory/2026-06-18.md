# 2026-06-18 工作日志

- 修复思绪编辑模态框标题不回填的bug：`<input id="mThoughtText">` 缺少 `value` 属性，导致 `defaultValue` 参数未赋值。添加 `value="${esc(defaultValue||'')}"` 修复。
- 习惯+待办合并为"行动"模块：底部导航变为 首页/能力/行动/思绪/工作轨（占位）。行动页3个tab（今日/库/归档），每个tab内待办区和习惯区可折叠。FAB弹出选择"新建待办"或"新建习惯"。
- 删除旧的 renderHabits/renderTodos 主体函数（保留为空壳），新增 renderAction/switchActionTab/toggleCollapse/showActionFabMenu。
- **工作轨模块完整实现**：
  - 数据层：store.js 新增 workEvents/workTags 字段 + 11个方法（CRUD/统计/导出/标签同步）
  - 视图A（鸟瞰）：热力图日历（月历+四级热力色）+ 空心圆环图（SVG莫兰迪6色）+ 下一步待办聚合
  - 视图B（聚焦）：微型周历条 + 纵向事件卡片流 + 药丸标签 + 4区手风琴折叠（工作内容/发现问题/成长/下一步）
  - 模态框：新增/编辑事件（手风琴表单+标签输入+下一步管理）、标签选择器、导出（时间范围→Markdown→剪贴板）
  - CSS：~250行工作轨专用样式（莫兰迪局部变量、日历/圆环图/卡片/手风琴/标签/表单）
  - SW版本 v5-4 → v5-5

- **工作轨UI/UX 6项修复**（SW v5-5 → v5-6）：
  1. 空状态插画：蓝绿热力图替换为人坐电脑前SVG插画
  2. 日历右箭头修复：`workCalNav`的`toISOString()`时区bug（UTC偏移导致月份不变），改用手动年月计算；新增"返回今天"按钮
  3. 标签添加修复：datalist拦截Enter键导致`addWorkModalTag`不触发，添加明确的"+"按钮
  4. 字段重命名：标题→事项，工作内容去掉事项字段，成长总结→成长
  5. 减少卡片感：事件卡片/圆环图/待办区去除background/border/border-radius，改用左侧3px彩色竖线有机风格
  6. 手风琴图标问号修复：`charAt(0)`截断emoji surrogate pair，改用`split(' ')`提取emoji

- **工作轨UI/UX 4项修复**（SW v5-6 → v5-7）：
  1. 事件卡片改为无卡片有机风格：去掉左边3px粗线，改为`border-bottom`轻量分隔线（同entry-card）；手风琴默认折叠（`renderAccordion`添加collapsed class）
  2. 热力图方格：事项数量从右下角移到右上角（`cell-count-top`），未完成下一步数量显示在左下角（`cell-pending-bottom`，半透明白底+深红色突出）
  3. 标签输入：彻底移除datalist（浏览器兼容性差），改为点击式标签提示pills（`work-tag-hint-pill`），Enter时加`stopPropagation()`防冒泡
  4. 空字段不显示：renderWorkEventCard中用`.filter(Boolean)`过滤空section；导出`buildWorkExportMd`同样跳过空section，下一步显示"✅ 已完成"/"⬜ 未完成"状态

- **工作轨4项UI修复**（SW v5-7 → v5-8）：
  1. 事件卡片整卡折叠：默认只显示事项标题+标签同行（flex-wrap换行），点击整卡展开全部内容（工作内容/问题/成长/下一步直接平铺，不再用手风琴二次折叠）
  2. 标签回车白屏：移除`onkeydown`内联属性，改用`addEventListener('keydown')`+`stopImmediatePropagation()`彻底拦截Enter
  3. 事件列表用bullet dot分条：左侧8px圆点（`.work-event-dot`）+flex布局，参考todo-card的排版风格
  4. 热力图方格加大：font-size 12→14px，角标数字 8→10px，grid gap 3→4px

- **工作轨事项卡片样式微调**（SW v5-8 → v5-9）：
  1. 圆点改为短竖线（3×16px，与专项proj-indicator一致）
  2. 分隔线透明度0.04→0.08更明显
  3. 卡片间距12px→14px更宽敞

- **热力图方格宽高比**：aspect-ratio 1→2/1（宽度减半，高度不变）

- **工作事件结构调整**（v5-9）：
  1. 工作内容新增"任务背景"字段（placeholder：安排人、原因、紧迫度）
  2. "我的贡献"改名为"阻力与行动"（placeholder：遇到困难及解决方法、协调人）
  3. "价值" placeholder改为"可量化价值、可复用模式"
  4. buildWMField支持自定义placeholder参数
  5. 新增/编辑/渲染/导出/Store默认值全部同步更新
