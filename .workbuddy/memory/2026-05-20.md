# 2026-05-20

## V5.0 首页全面升级

### 重塑箴言（顶部）
- 从解放脑条目加权随机抽取一条展示（★★★=3倍, ★★=2倍, ★=1倍, 无星=0.5倍）
- 解放脑新增 importance 字段（前向兼容自动补 0），编辑/新建弹窗支持权重设置
- 引用样式卡片展示，左侧绿色竖线

### 今日焦点（中部）
- **待办队列**：Top 2 未完成今日待办，行内圆形 checkbox 直接完成，完成后自动递补
- **习惯追踪**：Top 2 未打卡习惯，高权重且连续两天未打卡触发「断联」警示（橙色背景+徽章），强制置顶

### 近期聚焦（底部，7天窗口）
- **能力主线**：权重最高的未完成能力
- **活跃专项**：积分算法（已完成action×2 + 关联thought×1），最高分未完成专项
- **思绪焦点**：7天内标签频次 Top 1，点击跳转标签筛选
- **高频路障**：7天内有更新的未解决问题，按 occurrenceCount 降序 Top 2

### 数据模型扩展
- `liberationEntries` 新增 `importance` 字段
- 所有 entry 类型新增 `occurrenceCount`(默认1) 和 `updatedAt` 字段
- `store.js` 新增 `addProblemOccurrence()` 方法
- 问题卡片增加 +1 按钮，显示出现次数

### 文件改动
- `store.js`：数据模型补丁 + CRUD 方法更新
- `app.js`：renderHome 完全重写 + 解放脑编辑增强 + 问题+1按钮
- `css/style.css`：旧 .home-grid 移除，新首页三段式样式
- `sw.js`：v4-2 → v5-0

## V5.1 首页UI精确调整

按V酱提供的UI设计图重写首页样式和结构：

### 重塑箴言
- 背景改为淡紫→淡蓝渐变
- 左右大号引号装饰（❝ ❞）
- 文字居中放大（18px），底部标签「—— 关于解放脑」
- 三个轮播指示点

### 今日焦点（左右两栏）
- 左栏：📋 今日待办 Top 2（橙色标题）
  - 方形复选框 + 标题 + 星级 + 来源（能力·专项）
- 右栏：🔥 习惯追踪 Top 2（绿色标题）
  - 火焰图标circle + 标题 + 星级 + 断联标签 + 连续天数 + 圆形打卡按钮
  - 断联项红色警示：「再断1天将中断连击」
- 底部提示文字

### 近期聚焦（列表布局）
- 每项一行：左侧图标circle + 标签 + 标题 + 描述 + 右侧箭头
- 能力主线：👑 紫色 + 能力名 + "当前权重最高的核心能力"
- 活跃专项：⚡ 蓝色 + 专项名 + "活性积分 N" + 进度条
- 思绪焦点：🌿 绿色 + 标签名 + "出现 N 次"
- 高频路障：⚠️ 红色 + 排名1/2 + 问题名 + 次数

### 文件改动
- `css/style.css`：完全重写 .home-* 全部样式类
- `app.js`：renderHome 重写 + 新增 resolveTodoSource() + disconnectDays()
- `sw.js`：v5-0 → v5-1

## V5.2 首页UI细节调整（按V酱反馈）

### 1. 箴言更醒目
- 字体从 18px 放大到 22px，font-weight 600，letter-spacing 0.5px
- 引号放大到 56px，颜色更淡
- padding 上下增大

### 2. 去掉红框部分
- 箴言：去掉「—— 关于解放脑」标签 + 三个轮播点
- 今日焦点：去掉「完成后自动递补下一条」和「点击打卡·长按查看详情」底部提示
- 近期聚焦：去掉「当前权重最高的核心能力」等描述文字
- 活跃专项：去掉进度条

### 3. 近期聚焦改为 2x2 网格
- display: grid; grid-template-columns: 1fr 1fr
- 每个格子是独立白色圆角卡片
- 卡片顶部：图标circle + 标签
- 卡片中间：标题大字
- 卡片底部：数据或问题列表 + 箭头

### 4. 两栏高度对齐
- .home-focus-wrap: align-items: stretch
- .home-focus-col: display: flex; flex-direction: column
- .home-focus-cards: flex: 1 自动填充
- 卡片无外边距，用gap间距，避免数量不一致时高度差

### 5. 减少卡片感
- 所有卡片去掉 box-shadow，只用背景色区分
- 近期聚焦每个卡片独立（非大卡片包裹）
- 卡片内边距精简

### 文件改动
- `css/style.css`：重写 homepage 全部样式
- `app.js`：renderHome 结构调整（箴言/近期聚焦/组装部分）
- `sw.js`：v5-1 → v5-2

## V5.3 首页UI调整（按V酱反馈）

### 1. 今日焦点改为纵向布局
- 取消左右两栏并排，改为上下纵向排列（待办在上、习惯在下）
- .home-focus-wrap 改为 flex-direction: column
- .home-focus-col 去掉 flex:1 和 min-width:0
- .home-focus-cards 去掉 flex:1

### 2. 近期聚焦改为波浪线分割风格
- 去掉所有卡片样式（白色背景/圆角/箭头）
- 内容居中对齐（text-align: center + align-items: center + justify-content: center）
- 波浪线分割：SVG背景的伪元素（::before 垂直线 + ::after 水平线）
- 中心圆点装饰：.home-recent-center-dot 绝对定位
- 去掉所有 .home-recent-arrow 元素
- 小屏幕隐藏波浪线和圆点

### 文件改动
- `css/style.css`：今日焦点纵向 + 近期聚焦波浪线+居中
- `app.js`：去掉箭头 + 增加中心圆点div
- `sw.js`：v5-2 → v5-3

## V5.3.1 首页UI微调

### 1. 近期聚焦波浪线改为直线
- .home-recent-grid::before：去掉SVG波浪线，改为 width:1px + background: var(--border-light) 单条直线
- .home-recent-grid::after：同理改为单条水平直线

### 2. 高频路障问题样式
- .home-recent-problem-text：font-weight: 600（加粗）
- .home-recent-problem-count：font-size: 11px（缩小），font-weight: 400（不加粗），color: var(--text-tertiary)（变灰）

### 3. 习惯打卡按钮缩小
- .home-habit-check：28px → 22px
- .home-habit-check svg：14px → 12px

### 文件改动
- `css/style.css`：伪元素直线 + 路障文字样式 + 习惯按钮尺寸

## V5.3.2 首页UI + 思绪编辑优化

### 1. 弱化待办/习惯卡片感
- .home-focus-card：background 从 var(--bg-surface) 改为 rgba(255,255,255,0.35)，半透明更轻盈

### 2. 近期聚焦加整体卡片
- .home-recent-grid：加 background: var(--bg-surface) + border-radius: var(--r-lg) + padding
- 四个模块统一在一个白色圆角容器内，线分割保留

### 3. 箴言字体改为楷体
- .home-quote-text：font-family 加入 KaiTi, 楷体, STKaiti, 华文楷体

### 4. 思绪编辑弹窗优化
- 标题从 textarea 改为 input type="text"（单行，15px，加粗，淡底色）
- 内容（备注）从 rows=2 改为 rows=6 + min-height:120px + line-height:1.7
- placeholder 从"备注"改为"📝 内容（选填）"
- 手机上编辑长内容更方便

### 文件改动
- `css/style.css`：卡片透明度 + 近期聚焦整体卡片 + 楷体
- `app.js`：思绪编辑弹窗 title→input + note加大

## V5.3.3 近期聚焦线分割修复

### 问题
手机上伪元素十字线 z-index:1 显示在卡片内容之上，半透明卡片背景导致线穿透文字区域，显示很乱。

### 修复
- 去掉 .home-recent-grid::before/::after 伪元素
- 改用卡片自身的 border-right + border-bottom 实现线分割：
  - .home-recent-card:nth-child(1),:nth-child(2) { border-bottom }
  - .home-recent-card:nth-child(odd) { border-right }
- 中心圆点背景改为 rgba(255,255,255,0.35) 与容器一致
- 增大 .home-recent-card padding（12px → 16px 左右）
- 响应式：小屏幕去掉 border

### 文件改动
- `css/style.css`：线分割方式重构

## V5.3.4 近期聚焦等高等宽修复

### 问题
四个格子大小不一致：路障内容多撑开最大，能力/思绪内容少偏小。

### 修复
- .home-recent-grid：加 grid-template-rows: 1fr 1fr 强制等高
- .home-recent-card：加 min-height:0 + overflow:hidden，padding 16px
- 标题/副内容/标签/问题文字：加 max-width:100% + ellipsis，确保内容溢出用省略号
- .home-recent-card-problems：加 max-height:60px + overflow:hidden，防止问题列表溢出
- .home-recent-problem-text：加 min-width:0，确保 flex 子项可正确 ellipsis

### 文件改动
- `css/style.css`：grid 等高 + overflow 限制 + ellipsis

## V5.3.5 活跃专项积分计算修复

### 问题
活跃专项积分只计算了 action/problem/thought，没有计算待办（todos）。完成专项关联待办后积分不涨。

### 修复
- 在积分计算中新增遍历 appData.todos
- 匹配条件：status==='completed' && sourceCapId===cap.id && sourceProjId===proj.id && completedAt 在7天内
- 每条已完成待办 +2 分（与 action/problem 权重一致）

### 文件改动
- `app.js`：renderHome 活跃专项积分计算逻辑
