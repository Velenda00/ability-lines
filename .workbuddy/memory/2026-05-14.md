# 2026-05-14 V3.1 改动

## Phase 1: 基础修复
- 行动/问题/收获列表显示 importance（权重星星）和 note（备注预览💬）
- 所有备注字段从 `<input>` 改为 `<textarea rows="2">` 支持换行
- 行动完成逻辑：手动点✅才标记完成，不自动根据待办完成

## Phase 2: 待办关联重构
- 删除 `showEntryFromTodoModal`（过程关联条目）和 `showCreateTodoFromEntry`（旧版创建待办）
- `showEntryWithProcessModal` 删除过程选择区，简化为文本+权重+备注
- 行动卡片 buildGraphPanel 新增「📋 待办」和「⚠️ 问题」按钮
- `showAddTodoForEntry(entryId)` — 从行动卡片创建待办，自动关联sourceEntryId
- `showAddProblemFromEntry(entryId)` — 从行动卡片录入问题，自动创建discover关联
- 待办tab创建/编辑弹窗改为 `showTodoModalWithLink`，新增"关联到行动"树形选择器

## Phase 3: 关系图 + 折叠
- SVG关系图：线条stroke-width从1.5→1、viewBox高度230→260、节点文字加pill白背景、中心节点支持click进入编辑、外围节点也支持click
- 新增 `gMouseDown` + mousemove/mouseup 事件支持桌面端拖动
- 关联弹窗改多选（checkbox替代radio）+ 顶部显示已有关联（可删除✕）+ 添加后不关闭弹窗可继续
- 问题区和收获区标题栏可折叠（点击标题 toggleProjSection），显示箭头和数量

## Bug修复（二次）
- **行为列表缺权重/备注**：在 `proj-li-sub` 中加入 `impStars` 和 `proj-note-hint`
- **问题列表缺权重/备注**：同上，替换原来的 `proj-ist` 状态文字为 info 行
- **创建待办/问题权重无效**：`confirmTodoEntryM`/`confirmProblemEntryM` 的 onclick 硬编码了 `_imp=0`（闭包初始值），改为运行时从闭包变量读取
- **收获关联显示"发现问题"**：`addRelation` 重复检查改为双向匹配 `(fromId↔toId, type)`
- **关联后只能一次+按钮消失**：同上修复，addRelation不再误判反向同类型关联为重复
- **拖动提示过大**：移除 `proj-drag-hint` 元素

## Bug修复（三次）
1. **关联弹窗类型与目标不联动**：新增 `filterRelTargets()`，选类型后自动筛选显示对应分类的目标条目
2. **行为节点大小**：中心节点 w 固定为 1（不随权重变化），始终最大
3. **关系图关联线**：边标签加 onclick 跳转到目标条目；track 类型颜色从绿色#5a7d6b改为蓝色#4A90B8
4. **待办展示优化**：待办移到关系图前面，默认展开，加大字号，加进度条
5. **待办tab关联选择器**：重写为树形折叠结构（能力→专项→行为缩进），参考思绪关联弹窗
6. **收获-思绪双向同步（Phase 4）**：
   - 收获条目新增 `syncThoughtId` 字段
   - 思绪新增 `sourceReviewId/sourceReviewCapId/sourceReviewProjId` 字段
   - 新增 `showReviewEditModal`：收获编辑弹窗增加"归入思绪"开关
   - 新增 `syncReviewToThought`：收获→思绪同步
   - `Store.linkThought` 扩展：思绪关联到专项/行为时自动创建收获条目
   - 收获列表显示同步标记，思绪列表显示来源收获信息

## Bug修复（四次 - 2026-05-14 上午）
1. **关联弹窗滚动不到按钮**：modal-box 内容区加 `flex:1;overflow-y:auto` 容器，按钮固定底部
2. **折叠态待办徽章更明显**：`.proj-meta-todo` 样式增大（12px/加粗/蓝底白字），移除展开区重复进度条，只保留简洁待办列表
3. **待办关联行为不显示**：`showTodoModalWithLink` 中 `split('_')` 因 id 含下划线导致解析失败，改用 `split('|')` 分隔符
4. **问题/收获缺删除按钮+关联行为**：
   - 问题区添加 🗑️ 删除按钮 + `projDeleteEntry`
   - 收获区添加 🗑️ 删除按钮
   - 问题区显示关联行为（discover来源行为 / derive衍生行为）
   - 收获区显示关联行为（harvest来源行为）
   - 思绪tab移除 `sourceReviewId` 重复显示，只保留 `linkInfo` 统一关联展示
5. **衍生行为节点标注**：SVG关系图中 `derived` 节点文字标签前加「衍·」前缀

## Bug修复（五次 - 2026-05-14 下午）
1. **待办关联行为 — 三处修复**：
   - 待办卡片上「📎 来源」信息从折叠区移出，始终可见
   - 待办弹窗改为 flex 布局，取消内部树 max-height 限制，避免手机端双层滚动导致按钮不可达
   - 移除能力层/项目层标签的 `selTodoLink` 调用（无法保存关联但会误导用户以为已选中）
2. **收获纳入思绪重复卡片**：`syncReviewToThought` 不再调用 `Store.linkThought`（后者会自动创建收获条目导致重复），改为直接更新思绪的关联字段后统一 saveAll
3. **衍生行为卡片标注**：行为卡片的「衍生于」改为同时查找问题列表和行为列表，支持「问题→行为」和「行为→行为」两种衍生关系，并区分标注「衍生于问题：」和「衍生于行为：」

## Bug修复（六次 - 2026-05-14 晚上）
1. **待办关联行为双向不显示（问题1）**：
   - `Store.init` 旧数据兼容性：为旧待办补全 `sourceCapId/sourceProjId/sourceEntryType/sourceEntryId` 字段（设为 null）
   - `renderTodoCard` 增强：关联来源显示完整路径 `能力 > 专项 > 行为`；若关联条目找不到显示红色提示「关联条目未找到（可能已删除）」
2. **移除「关联思绪」小字提示（问题2）**：移除5处条目/卡片下方的「💭 关联思绪：...」行，保留徽章形式（💭数量）
   - 能力卡片、行为区条目、问题区条目、收获区条目、待办卡片折叠区
3. **关系图标注区分方向（问题3）**：`buildGraphData` 中 derive 关系的边标签根据方向区分
   - 从源→衍生：标注「衍生行为」
   - 从衍生→源：标注「源头行为」
